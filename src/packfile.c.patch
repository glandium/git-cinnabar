diff --git a/packfile.c b/packfile.c
index 7fbe47ca18..d5cf62ce33 100644
--- a/packfile.c
+++ b/packfile.c
@@ -2081,8 +2081,10 @@ off_t nth_packed_object_offset(const struct packed_git *p, uint32_t n)
 	}
 }
 
-off_t find_pack_entry_one(const struct object_id *oid,
-			  struct packed_git *p)
+off_t real_find_pack_entry_one(const struct object_id *oid,
+			       struct packed_git *p);
+off_t real_find_pack_entry_one(const struct object_id *oid,
+			       struct packed_git *p)
 {
 	const unsigned char *index = p->index_data;
 	uint32_t result;
